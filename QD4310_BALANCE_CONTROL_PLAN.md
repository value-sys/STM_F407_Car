# QD4310 水管平衡滚球控制开发计划

## 1. 文档目的

本文档用于指导水管平衡滚球平台的开发，也用于后续对话和团队成员交接。核心目标是把视觉检测到的小球位置转换为水管姿态，再转换为 QD4310 的目标电机角度，最终完成电赛题目中的小球定位和保持。

当前工程已经具备 QD4310 驱动库、视觉通讯、OLED、按键和 FreeRTOS 基础，但还没有完成 QD4310 实例化、平台标定和视觉闭环控制。

相关代码目录：

- `Module/motor/qd4310/`
- `User/App/vision_task.c/.h`
- `User/Device/vision_protocol.c/.h`
- `User/App/ui_task.c/.h`

## 2. QD4310 的职责

QD4310 是执行机构，不负责判断小球位置，也不负责决定比赛目标。

完整系统分工如下：

```text
OLED/按键
    -> 选择比赛模式、目标位置和启动/停止

视觉
    -> 输出小球实际位置、有效标志、置信度和时间戳

平衡控制器
    -> 计算小球位置误差和速度
    -> 计算水管需要的倾角

QD4310 任务
    -> 接收目标电机角度
    -> 做角度、速度、加速度限幅
    -> 通过 USART2 发送命令
    -> 接收并解析电机反馈
    -> 通信异常或超限时停止
```

当前 QD4310 的 C/C++ 驱动已经加入工程，但还没有形成真正的 `QD4310Task`，也没有接入视觉位置控制闭环。

## 3. 首先必须确定的三个标定结果

### 3.1 电机和平台的安全运动范围

不能只看 QD4310 理论上的 `0~360` 度，需要测量：

```text
电机理论角度范围
机械结构允许范围
实际控制安全范围
```

需要得到：

```text
phi_safe_min：平台安全下限对应的电机角度
phi_safe_max：平台安全上限对应的电机角度
```

测试要求：

1. 使用极低速度或小角度步进。
2. 接近机械极限前提前停止。
3. 禁止让电机硬撞合页、连杆或水管限位。
4. 最终软件限幅要比机械极限小，留出安全余量。
5. 每次上电先保持安全停止，不要直接发送大角度命令。

建议记录表：

| 项目 | 数值 | 备注 |
|---|---:|---|
| QD4310 电机理论最小角度 |  | 由手册和驱动接口确定 |
| QD4310 电机理论最大角度 |  | 由手册和驱动接口确定 |
| 水管机械最低位置 |  | 禁止继续向下 |
| 水管机械最高位置 |  | 禁止继续向上 |
| 软件安全最小角度 |  | 低于机械极限 |
| 软件安全最大角度 |  | 低于机械极限 |

### 3.2 平衡角度

记水管水平、小球能够保持在中心附近时的电机角度为：

```text
phi_balance
```

它不一定是电机的机械零度，因为会受到以下因素影响：

- 电机安装误差。
- 合页和连杆安装误差。
- 水管自身重量。
- 电机减速机构间隙。
- 水管变形。
- 小球和水管之间的摩擦。

标定方法：

1. 视觉确认小球位于水管中心。
2. 使用 QD4310 小角度调整水管。
3. 找到水管基本水平、小球不会明显向一侧持续滚动的位置。
4. 记录电机反馈角度为 `phi_balance`。
5. 从此以后，控制器输出都围绕 `phi_balance` 调整。

### 3.3 电机角度到水管倾角的映射

QD4310 输出的是电机角度 `phi`，控制器真正需要的是水管倾角 `alpha`。二者不是天然相等：

```text
电机角度 phi
    -> 曲柄/连杆/摇臂位移
    -> 水管左端高度变化
    -> 水管倾角 alpha
```

在平衡点附近可以先近似：

```text
phi_cmd = phi_balance + K_phi * alpha_cmd
```

但 `K_phi` 必须实测。更可靠的方式是建立标定表：

```text
电机角度 -> 左端高度 -> 水管倾角
```

如果向上和向下存在明显摩擦差异，可以建立两套映射：

```text
左侧升高方向映射
左侧降低方向映射
```

## 4. 坐标系约定

当前机械描述为：

- 水管右端固定在合页上。
- QD4310 位于水管左侧。
- 电机改变水管左侧高度。
- 左侧升高时，小球向右滚动。
- 左侧降低时，小球向左滚动。

建议统一定义：

```text
x = 小球从右向左的距离
x = 0：水管最右端
x 增大：向左
alpha > 0：水管左侧升高
alpha < 0：水管左侧降低
```

根据该定义：

```text
alpha > 0 -> 小球向右 -> x 减小
alpha < 0 -> 小球向左 -> x 增大
```

所有视觉、控制器和 QD4310 方向都必须服从这个约定。第一次测试时需要用一个很小的电机角度变化确认实际方向，如果方向相反，只调整方向符号，不要同时修改 PID 参数。

## 5. 视觉数据如何参与控制

视觉至少提供：

```text
ball_position：小球位置
valid：位置是否有效
confidence：识别置信度
timestamp：视觉时间戳
sequence：帧序号
```

单片机已经能够根据连续位置计算小球速度：

```text
ball_speed = position_difference / time_difference
```

建议平衡控制器使用：

```text
x：滤波后的小球实际位置
v：滤波后的小球实际速度
x_target：当前目标位置
```

视觉数据不能直接控制电机。正确的控制链是：

```text
视觉位置
    -> 有效性/置信度/超时检查
    -> 位置滤波
    -> 速度计算
    -> 位置控制器
    -> 水管目标倾角
    -> 电机角度映射
    -> QD4310 角度命令
```

视觉无效、置信度过低或超过超时时间时，必须进入安全状态：

```text
停止新的角度变化命令
或者回到 phi_balance
或者按预设安全策略停车
```

具体采用哪种策略，需要结合机械结构和比赛安全性确定。开发初期建议先停止输出或回到平衡角度，不要保持上一次危险角度无限运行。

## 6. 位置误差和角度控制方向

定义位置误差：

```text
e = x - x_target
```

在当前坐标系下：

### 实际位置大于目标位置

```text
x > x_target
e > 0
```

小球太靠左，需要让小球向右运动，因此：

```text
水管左侧升高
alpha 增大
QD4310 向“左侧升高”的方向转动
```

### 实际位置小于目标位置

```text
x < x_target
e < 0
```

小球太靠右，需要让小球向左运动，因此：

```text
水管左侧降低
alpha 减小
QD4310 向相反方向转动
```

不能简单地写成“实际大于目标就转到一个固定角度”。应该根据误差大小和小球速度连续调整。

推荐的第一版控制公式：

```text
alpha_cmd = alpha_balance + Kp * e + Kd * v
```

其中：

- `alpha_balance`：水管平衡倾角，通常接近 0，但不要强行假定为 0。
- `Kp * e`：根据位置误差改变水管倾角。
- `Kd * v`：根据小球运动速度提前刹车。

如果小球正在向左运动，`v > 0`，微分项会增加左侧升高动作，帮助阻止它继续向左冲；如果小球正在向右运动，`v < 0`，控制器会减少左侧升高动作。

然后把水管倾角转换为电机角度：

```text
phi_cmd = Mapping(alpha_cmd)
```

最后执行限幅：

```text
phi_cmd = clamp(phi_cmd, phi_safe_min, phi_safe_max)
```

还需要限制目标角度的变化速度，避免 QD4310 突然大幅动作。

## 7. 摩擦力和左右方向不一致的处理

摩擦力不一样时，不应该修改比赛目标位置。例如，不能因为摩擦大就把 `+5 cm` 改成 `+3 cm`。

目标位置仍然必须是比赛要求的位置，摩擦补偿放在执行器控制中。

推荐逐步加入：

### 7.1 方向补偿

```text
左侧升高时使用一个补偿角度
左侧降低时使用另一个补偿角度
```

### 7.2 静摩擦死区补偿

当误差较大但水管没有动作时，增加最小启动角度；当误差接近零时取消该补偿，避免持续抖动。

### 7.3 速度限制和加速度限制

不允许根据视觉噪声让电机目标角度快速来回跳变。目标角度应该经过：

```text
角度限幅
角速度限幅
加速度限幅
```

### 7.4 方向独立 PID 参数

如果左移和右移的响应差异明显，可以使用：

```text
向左运动参数
向右运动参数
```

但必须在基础 PID 稳定后再增加，不能一开始同时引入太多参数。

## 8. 题目第三项的目标位置

题目要求：小车静止时，小球从中心点 `O` 出发，运行到 `+5 cm`，折返到 `-5 cm` 并稳定，运行时间不超过 5 秒，两个目标位置的最大误差绝对值不超过 1 cm。

如果水管长度为 25 cm，右端为 0，向左为正：

```text
右端       = 0 cm
中心 O     = 12.5 cm
+5 cm目标  = 17.5 cm
-5 cm目标  = 7.5 cm
```

因此目标序列为：

```text
12.5 cm -> 17.5 cm -> 7.5 cm
```

不建议直接存储某个固定电机角度作为目标。UI 或比赛模式应该存储相对中心的位置：

```c
target_offset_mm = +50.0f;
target_position_mm = center_position_mm + target_offset_mm;
```

电机角度由控制器根据实时误差计算得到。

## 9. 第二个 UI 模式的建议状态机

当前 UI 中的 `UI_MODE_STATIC_POSITION` 可以作为小球定点控制模式。

建议状态：

```text
STATIC_READY
    -> MOVE_TO_PLUS_5
    -> HOLD_PLUS_5
    -> MOVE_TO_MINUS_5
    -> HOLD_MINUS_5
    -> COMPLETE
```

每个状态需要保存：

```text
当前目标位置
目标位置偏移量
进入状态时间
稳定开始时间
是否已经稳定
```

建议的内部稳定判定：

```text
abs(position_error) <= 10 mm
并且 abs(ball_speed) 小于速度阈值
持续若干个控制周期
```

题目给出的 `±1 cm` 是最终误差要求，内部控制可以使用更严格的阈值，例如 5~8 mm，但具体数值要通过实测调整。

## 10. 分阶段开发和验收顺序

### 阶段 0：确认机械和坐标

目标：把机械方向说清楚。

需要确认：

1. QD4310 的正角度方向。
2. 电机正转时左侧是升高还是降低。
3. 水管右端是否真正固定。
4. 合页转轴是否水平。
5. 视觉位置 0 点对应哪个物理位置。
6. 视觉位置正方向是否与软件约定一致。

验收：手动给一个很小的角度，能预测小球运动方向。

### 阶段 1：QD4310 单独动作

目标：不接视觉，只验证电机和平台。

测试命令：

```text
使能
停止
小角度正向
回到平衡角
小角度反向
```

验收：电机动作方向正确，没有撞限位，能回到 `phi_balance`。

### 阶段 2：安全范围和角度映射

目标：得到：

```text
phi_safe_min
phi_safe_max
phi_balance
Mapping(alpha)
```

验收：任意控制命令都不会超过安全范围，且水管姿态变化可预测。

### 阶段 3：中心保持

目标：目标固定在中心，只让小球保持在中心附近。

先使用：

```text
P 控制
```

再逐步加入：

```text
速度反馈
D 项
摩擦补偿
```

验收：视觉有效时小球不持续跑向一侧，视觉失效时电机进入安全状态。

### 阶段 4：小范围位置移动

目标依次测试：

```text
中心 -> +10 mm
中心 -> -10 mm
中心 -> +30 mm
中心 -> -30 mm
```

验收：运动方向正确、不过度冲击、不产生持续振荡。

### 阶段 5：题目第三项

目标：

```text
中心 -> +50 mm -> -50 mm
```

验收：

- 总时间不超过 5 秒。
- 到达 `+50 mm` 后能够折返。
- 到达 `-50 mm` 后能够稳定。
- 最终误差绝对值满足 10 mm 要求。

### 阶段 6：加入 UI 和完整安全逻辑

第二个 UI 模式建议：

- `KEY1`：开始或从暂停状态恢复。
- `KEY2`：完全停止。
- `KEY3`：退出当前模式并切换模式。
- `KEY4`：返回待机。

视觉无效、置信度不足、通信超时、QD4310 故障或角度超限时，均应停止控制或回到安全角度。

## 11. Ozone 和 VOFA 调试变量

### 11.1 QD4310 需要观察

```text
QD4310 目标角度
QD4310 当前角度
QD4310 目标速度
QD4310 当前速度
QD4310 通信状态
QD4310 故障状态
QD4310 限位状态
```

### 11.2 平衡控制器需要观察

```text
视觉实际位置
目标位置
位置误差
小球速度
目标水管倾角
目标电机角度
实际电机角度
P项输出
D项输出
摩擦补偿输出
最终限幅后输出
```

### 11.3 安全状态需要观察

```text
视觉有效标志
视觉置信度
视觉超时计数
QD4310通信错误计数
当前比赛状态
当前目标位置
是否暂停
是否完成
```

VOFA 适合画连续曲线，建议至少发送：

```text
position
target
error
speed
motor_angle_target
motor_angle_actual
```

## 12. 当前需要确认的机械问题

开始写平衡控制器前，需要确认：

1. 水管的实际有效长度是多少？
2. 视觉输出单位是 mm、cm 还是像素？
3. 视觉 0 点对应水管哪一端？
4. 视觉位置正方向是否为向左增加？
5. QD4310 电机正角度时，左侧是升高还是降低？
6. 电机与水管之间是直接连接、齿轮、曲柄还是连杆？
7. QD4310 输出轴和水管转轴之间是否有减速比？
8. 水管左右两端的支撑方式是什么？
9. 水管最大允许倾角是多少？
10. 小球直径、质量和水管内壁材料是什么？
11. 视觉最高帧率和实际稳定帧率是多少？
12. QD4310 反馈的是电机轴角度还是减速后输出轴角度？

这些参数决定 `phi_balance`、角度映射、PID 参数和摩擦补偿，不能仅靠代码猜测。

## 13. 最终控制结构

建议最终结构如下：

```text
UI_TASK
    -> 设置比赛模式和目标位置

VISION_TASK
    -> 接收位置、有效标志、置信度
    -> 计算小球速度

BALANCE_TASK
    -> 读取目标位置和视觉数据
    -> 计算位置误差
    -> 计算水管目标倾角
    -> 转换为 QD4310 目标角度
    -> 做限幅、斜坡和安全检查

QD4310_TASK
    -> 按固定周期发送角度/速度命令
    -> 接收并解析反馈
    -> 检查通信和故障

SAFETY
    -> 视觉异常、角度超限、通信异常时停止
```

核心原则：

1. 比赛目标位置由 UI/比赛状态机保存。
2. 电机角度不是比赛目标，而是控制器的实时输出。
3. 目标位置不能为了补偿摩擦而随意改变。
4. 实际位置大于目标位置时，应根据当前坐标系让小球向目标方向运动，并使用速度项提前制动。
5. 所有电机命令必须经过安全角度和速度限幅。
6. 视觉失效时不能继续沿用上一次危险输出。

